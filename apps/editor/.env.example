# No configuration is required. The editor uses this repository's data/ directory.
# For disposable QA fixtures only, override with an absolute path:
# POKEPC_DATASET_DIR="/absolute/path/to/disposable/data"
