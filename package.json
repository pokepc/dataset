{
  "name": "@pokepc/dataset",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'Not implemented yet'",
    "dev": "PORT=4011 bun run 'viewer/**/*.html'",
    "lint": "pnpm typecheck &&bunx --bun madge --circular --extensions ts,tsx lib/ tests/",
    "test": "vitest --run --bail 20",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "preact": "^10.28.4",
    "yolodb": "^1.2.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}